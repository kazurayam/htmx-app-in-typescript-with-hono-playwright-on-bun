{
  "name": "@kazurayam/htmx-app-in-typescript-with-hono-playwright-on-bun_my-app",
  "main": "src/index",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot ./src/index.tsx",
    "testdev": "bun run --hot ./src/index.tsx & bun test ./src/index.test.ts;   kill $(ps aux | grep '[0-9] bun run --hot ./src/index.tsx' | awk '{print $2}')",
    "main": "bun run --hot ./src/main.tsx",
    "testmain": "bun run --hot ./src/main.tsx & bun test;                        kill $(ps aux | grep '[0-9] bun run --hot' | awk '{print $2}')",
    "testsection": "bun run --hot ./src/main.tsx & bun test ./e2e/section4.test.ts; kill $(ps aux | grep '[0-9] bun run --hot' | awk '{print $2}')"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.14",
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.9.0",
    "@playwright/test": "^1.60.0",
    "@types/bun": "latest",
    "bun-types": "^1.3.14",
    "playwright-chromium": "^1.60.0",
    "playwright-core": "^1.60.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
